namespace Rythmbox.App.ViewModels;

/// <summary>Main application pages (mirrors old DrumStage <c>Page</c> enum).</summary>
public enum AppPage
{
    Machine,
    Pads,
    Mixer,
    Editor,
    Macro,
    Settings,
    Chrome,
}
