using Avalonia.Controls;

namespace Rythmbox.SampleCreator.Views.LayerEngine;

/// <summary>One velocity layer rendered as a self-contained card: header, hover actions,
/// and the round-robin samples that belong to it.</summary>
public partial class VelocityLayerCardView : UserControl
{
    public VelocityLayerCardView()
    {
        InitializeComponent();
    }
}
